import { PageNav } from '@/components'

const PageHeader: React.FC = () => {
  return (
    <header className={'page-section-px py-4'}>
      <PageNav />
    </header>
  )
}

export default PageHeader
